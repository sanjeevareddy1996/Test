  
orbs:
  node: circleci/node@2.0.2
  aws-cli: circleci/aws-cli@2.0.0
jobs:
  job1:
    machine:
      enabled: true
    steps:
      - run:
          name: Test.
          command: ssh -i "Test.pem" ec2-user@ec2-13-126-40-218.ap-south-1.compute.amazonaws.com 'echo "welcome to innobit" ';
version: 2.1
workflows:
  build_and_deploy:
    jobs:
      - Test:
          filters:
            branches:
              only: test4
              